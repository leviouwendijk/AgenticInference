public extension AgentInferenceStrategyIdentifier {
    static let direct = Self(
        rawValue: "direct"
    )

    static let native_reasoning = Self(
        rawValue: "native_reasoning"
    )

    static let sampled = Self(
        rawValue: "sampled"
    )

    static let refining = Self(
        rawValue: "refining"
    )
}
